export enum Sex {
  MAN = 0,
  WOMAN = 1,
}

export enum Roles {
  ADMIN = 'admin',
  MANAGER = 'manager',
  USER = 'user',
}

export enum ExperienceTypes {
  BEFORE_ONE_YEAR = 0,
  BEFORE_THREE_YEAR = 1,
  BEFORE_SIX_YEAR = 2,
  BEFORE_TEN_YEAR = 3,
}
